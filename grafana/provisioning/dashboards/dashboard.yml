apiVersion: 1

providers:
  - name: 'Auth Service Dashboards'
    orgId: 1
    folder: 'Auth Microservice'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: true

